#include <stdio.h>
#include <math.h>

int main() {
    // int(input()) + input()
    // scanf("%i", &x); // i == int, d == decimal
    // scanf("%lf", &x); // lf == long float == double
    printf("%lf", cos(1.0/2.0));
    return 0;
}