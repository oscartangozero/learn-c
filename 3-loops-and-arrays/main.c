#include <stdio.h>

int main() {
    int x = 80;
    scanf("%d", &x);


    for (int x = 10; x < 20;++x) {
        printf(...);
    }


    int x = 10; // scanf(x)
    while (x<20) {
        print(...);
        x++;
    }
}